# Games — engine probes, not games

The folders here are test-beds a session uses to measure the engine: `DepthProbe` measures
layerDepth sorting on a native build, `UiProbe` exercises the UI tree, `ChibiProbe` MakeChibi.
Real games live in their own repositories, seeded by `.claude/skills/new-game`. Nothing here ships.
