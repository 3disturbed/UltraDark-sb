# Couch Co-op

Local multiplayer: join by pressing a button, one shared camera, per-player devices.

Needs `input-mapping`, which is installed with it. Split-screen is not included — see the
`AGENT.md`.
