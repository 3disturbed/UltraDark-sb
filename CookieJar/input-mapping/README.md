# Input Mapping

Rebindable keyboard, mouse and gamepad controls, saved between runs.

Install it with `install_cookie`, or from the editor's Cookie Jar panel, then drop an
`InputSettings` component on any actor in the start scene.
